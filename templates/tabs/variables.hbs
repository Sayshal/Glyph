<div class="tab scrollable glyph-tab-placeholder{{#if tab.active}} active{{/if}}" data-group="{{tab.group}}"
  data-tab="{{tab.id}}">
  <p class="hint">{{localize "GLYPH.PLACEHOLDER.variables"}}</p>
</div>
